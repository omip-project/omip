# Simulator

The OMIP simulator generates reproducible multi-sensor mission data for several
vehicle domains.

A simulation configuration includes:

- vehicle ID;
- vehicle type;
- vehicle profile;
- scenario;
- mission ID;
- duration;
- transport;
- random seed;
- parameter overrides.

The Dashboard and command line use the same simulation configuration model.
